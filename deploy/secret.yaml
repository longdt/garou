# TEMPLATE — do NOT commit real secrets.
# Generate the jwt-secret value with:
#   echo -n "$(openssl rand -hex 32)" | base64
# Generate TLS certificates with cert-manager or:
#   openssl req -x509 -newkey rsa:4096 -keyout server.key -out server.crt -days 365 -nodes
apiVersion: v1
kind: Secret
metadata:
  name: garou-secrets
  namespace: garou
type: Opaque
data:
  # base64-encoded 32-byte hex string (replace with real value)
  jwt-secret: Y2hhbmdlX21lX2JlZm9yZV9wcm9kdWN0aW9u
  # base64-encoded TLS certificate and key (replace with real values)
  # tls-cert: <base64 PEM>
  # tls-key:  <base64 PEM>
